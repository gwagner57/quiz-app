class AnswerOption {
    constructor(sequenceNo, text, feedback, isCorrect, gradePoints) {
      this.sequenceNo = sequenceNo;
      this.text = text;
      this.feedback = feedback;
      this.isCorrect = isCorrect;
      this.gradePoints = gradePoints;
    }
}